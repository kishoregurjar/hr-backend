"use strict";

const SUPER_ADMIN_DASHBOARD_CONSTANTS = Object.freeze({
  ERROR_CODES: Object.freeze({
    COMPANY_NOT_FOUND: "SUPER_ADMIN_COMPANY_NOT_FOUND",
  }),
});

module.exports = {
  SUPER_ADMIN_DASHBOARD_CONSTANTS,
};
