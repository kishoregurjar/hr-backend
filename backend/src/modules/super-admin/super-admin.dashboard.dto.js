"use strict";

const buildDashboardResponse = (statistics) => ({
  statistics,
});

const buildCompanyStatisticsResponse = (statistics) => ({
  statistics,
});

module.exports = {
  buildDashboardResponse,
  buildCompanyStatisticsResponse,
};
