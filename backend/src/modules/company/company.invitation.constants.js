"use strict";

const COMPANY_INVITATION_CONSTANTS = Object.freeze({
  TOKEN: Object.freeze({
    BYTE_LENGTH: 32,
  }),

  EXPIRATION: Object.freeze({
    DAYS: 7,
  }),

  EMAIL: Object.freeze({
    MAX_LENGTH: 320,
  }),

  MAX_PENDING_INVITATIONS_PER_COMPANY: 100,

  ERROR_CODES: Object.freeze({
    INVITATION_NOT_FOUND: "INVITATION_NOT_FOUND",
    INVITATION_EXPIRED: "INVITATION_EXPIRED",
    INVITATION_ALREADY_ACCEPTED: "INVITATION_ALREADY_ACCEPTED",
    INVITATION_REVOKED: "INVITATION_REVOKED",
    INVITATION_ALREADY_EXISTS: "INVITATION_ALREADY_EXISTS",
    INVITATION_LIMIT_REACHED: "INVITATION_LIMIT_REACHED",
    INVITATION_EMAIL_MISMATCH: "INVITATION_EMAIL_MISMATCH",
    INVITATION_CREATE_FAILED: "INVITATION_CREATE_FAILED",
  }),
});

module.exports = {
  COMPANY_INVITATION_CONSTANTS,
};
